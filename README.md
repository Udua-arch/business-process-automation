# Business Process Automation System

A mini workflow automation project built in Python (Flask + SQLite) that simulates how business tasks can be automatically routed based on business rules.

## Features
- Create and view business tasks
- Automatic task assignment (Manager or Employee)
- Simple database with SQLite
- Web interface using Flask

## Run the Project
```bash
pip install flask sqlalchemy
python app.py
